// datasource db {
//   provider = "postgresql"
//   url      = env("DATABASE_URL")
// }

// generator client {
//   provider = "prisma-client-js"
// }

generator client {
  provider = "prisma-client-js"
  previewFeatures = ["referentialIntegrity"]
}
datasource db {
  provider = "mysql"
  url = "mysql://4h9sgagvdstofi4295s3:pscale_pw_adRuiUt8AY1exUgGOW95PRQztAuM0dkS3GHocGFyl9G@ap-northeast.connect.psdb.cloud/book12?sslaccept=strict"
  referentialIntegrity = "prisma"
}

model Category {
  id            Int   @id @unique   @default(autoincrement())
  name          String
  slug          String @unique
  books         Book[]
}

model Book {
  id            Int     @id @unique  @default(autoincrement()) 
  createAt     DateTime  @default(now())
  updateAt     DateTime  @updatedAt
  deleteAt     DateTime?
  title         String
  description   String?
  coverImg      String?
  author        User    @relation(fields: [authorId], references: [id], onDelete: Cascade)
  authorId      String
  category      Category @relation(fields: [categoryId], references: [id], onDelete: Cascade)
  categoryId    Int

  @@index([authorId, categoryId])
}

model User {
  id            String   @id @unique    @default(uuid())
  createAt     DateTime  @default(now())
  updateAt     DateTime  @updatedAt
  deleteAt     DateTime?
  name          String
  email         String   @unique
  password      String  
  coverImg      String?
  books        Book[]  
}
